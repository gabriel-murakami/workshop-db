apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-catalog-service-deployment
  namespace: workshop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: db-catalog-service
  template:
    metadata:
      labels:
        app: db-catalog-service
    spec:
      containers:
        - name: postgres-catalog-service
          image: postgres:15
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: db-catalog-service-secret
